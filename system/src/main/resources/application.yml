spring:
  application:
    name: system
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/courseimooc?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
server:
  port: 9001
  servlet:
    context-path: /system
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/

mybatis:
  mapper-locations: classpath:/mapper/*.xml